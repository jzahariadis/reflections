What happens when you initialize a repository? Why do you need to do it?
 a git folder is created that track files you choose

How is the staging area different from the working directory and the repository? What value do you think it offers?
 staging area doesnt have commited changes yet but you can track the files in  staging area

How can you use the staging area to make sure you have one commit per logical change?
 you can add all the file you think is logical do the changes and then commit them

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
 whne you try something like a new feature and you are not sure if it will work or if you want to include it in the final project you can use branching,
 it might help in collaboration as well and merge files in the end

How do the diagrams help you visualize the branch structure?
 diagrams help as visualize the parent and child branches

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
 the result is that one of the two branches name is deleted but the commit history remains and merges with the other branch in time order. all the commits remain reachable by the head the last commit

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
 when there is no conflict git can auto merge branches so you can save time and makes collaboration and developing easy
 git can always do this and sometimes we need to specify what changes we want to commit manualy